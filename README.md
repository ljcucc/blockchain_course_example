# run this in bash terminal

```
npm i # install dependencies
npx hardhat node # run local blockchain server
npx hardhat run ./scripts/deploy-and-setup.js --network localhost # deply contract
python3 -m http.server 8080
```
